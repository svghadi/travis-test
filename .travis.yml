os: linux

group: edge

script:
  - env
  - java -version
  - mvn --version

language: java


jobs:
  include:
    - arch: ppc64le
      dist: bionic
      jdk: openjdk15
    - arch: ppc64le
      dist: focal
      jdk: openjdk16
    # xenial will be deprecated in future
    - arch: ppc64le
      dist: xenial
      jdk: openjdk8

