os: linux

#group: edge
install: skip
script: skip

language: erlang

jobs:
  include:
    - arch: ppc64le
      dist: bionic
      otp_release: "23.0.2"
    - arch: ppc64le
      dist: focal
      otp_release: "25.1.1"
    - arch: amd64
      dist: bionic
      otp_release: "23.0.2"
    - arch: amd64
      dist: focal
      otp_release: "25.1.1"
      
