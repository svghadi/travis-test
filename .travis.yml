os: linux

language: clojure

install: lein help

script: 
  - lein new app hello
  - cd hello
  - lein run
  
jobs:
  include:
    - arch: amd64
      dist: bionic
    - arch: amd64
      dist: focal
    - arch: ppc64le
      dist: bionic
    - arch: ppc64le
      dist: focal
